package org.ia.rxthorax.data.preprocess;

public class ImageResizer {

    public String resize(String imageReference, int width, int height) {
        return "Resized image placeholder for " + imageReference + " to " + width + "x" + height;
    }
}
